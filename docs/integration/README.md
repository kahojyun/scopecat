# Integration

## Status

Sentinel for cross-subsystem design.

## Purpose

This directory owns contracts that cut across subsystem boundaries: data
ownership, dependency rules, composition rules, domain boundary notes,
cross-system contracts, and release staging.

Do not place subsystem-owned domain concepts here. Integration documents should
reference the owning subsystem instead.
