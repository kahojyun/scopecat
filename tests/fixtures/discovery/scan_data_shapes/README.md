# Scan Data Shape Fixtures

These fixtures are bounded discovery evidence for primary-data shape review.
They are repository-safe synthetic examples, not portable/export artifacts or
public schema contracts.

`shape-input.json` files use
`fixture_classification: repository_safe_synthetic_discovery_input`.
`expected-shape-summary.json` files use
`summary_classification: internal_validation_summary`.
