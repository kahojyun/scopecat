[project]
name = "qa-chevron-calibration"
requires-python = ">=3.11,<3.13"
dependencies = [
  "not a valid dependency entry",
  "numpy==1.26.4",
  "qcodes>=0.46",
]

[dependency-groups]
analysis = ["scipy>=1.11"]
