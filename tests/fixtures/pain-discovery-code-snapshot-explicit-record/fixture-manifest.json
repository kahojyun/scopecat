{
  "fixture_id": "pain-discovery-code-snapshot-explicit-record",
  "status": "draft pain-discovery fixture",
  "purpose": "Test whether explicit code snapshot and entrypoint recording solve most manual provenance pain before a code registry or managed runner exists.",
  "grounding": {
    "direct_sample": [
      "copied folders and backup variants",
      "duplicated helper modules",
      "mutable or ambiguous notebooks",
      "hardcoded paths and local runtime assumptions",
      "weak canonical identity for copied assets"
    ],
    "user_clarified_desired_records": [
      "explicit selected entrypoint",
      "hash-based code snapshot",
      "experiment template version history"
    ],
    "future_hypotheses_out_of_scope": [
      "managed code registry",
      "system-managed bare git repository",
      "uv.lock as required dependency contract",
      "temporary run folder execution",
      "managed runner"
    ]
  },
  "redaction_policy": {
    "source": "public-test-fixture",
    "forbidden_content": [
      "real absolute paths",
      "usernames",
      "machine labels",
      "network addresses",
      "hardware identifiers",
      "real sample labels"
    ]
  },
  "artifacts": [
    {
      "path": "code/experiment_template.py",
      "role": "experiment template",
      "handling": "text-only; must not execute"
    },
    {
      "path": "code/helper_waveforms.py",
      "role": "helper module",
      "handling": "text-only; must not execute"
    },
    {
      "path": "snapshots/snapshot-v1.json",
      "role": "code snapshot"
    },
    {
      "path": "snapshots/snapshot-v2.json",
      "role": "code snapshot"
    },
    {
      "path": "snapshots/copied-candidate.json",
      "role": "copied candidate with unknown canonical status"
    },
    {
      "path": "records/run-record.json",
      "role": "explicit run provenance record"
    },
    {
      "path": "notebook-evidence/cell-map.json",
      "role": "ambiguous notebook evidence"
    }
  ],
  "fixture_questions": [
    "Does explicit snapshot and entrypoint recording cover the core code-version pain?",
    "Which remaining fields are still painful without a real code registry?",
    "Which information must be user-declared instead of inferred from arbitrary code?"
  ]
}
