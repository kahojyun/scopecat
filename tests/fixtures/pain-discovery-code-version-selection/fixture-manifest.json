{
  "fixture_id": "pain-discovery-code-version-selection",
  "status": "draft pain-discovery fixture",
  "purpose": "Test whether users need to select and possibly load a previous code version or branch variant for an experiment, instead of only exporting the code snapshot used by a past run.",
  "grounding": {
    "direct_sample": [
      "copied code folders",
      "renamed file variants",
      "helper modules with weak canonical identity",
      "notebook and script provenance ambiguity"
    ],
    "user_clarification": [
      "snapshot export helps retrospective analysis but may not improve experiment QoL",
      "users need to choose a suitable older code version or branch variant",
      "loading the selected version for execution is the clearer QoL improvement",
      "isolated process execution is plausible but not yet accepted architecture"
    ],
    "future_hypotheses_out_of_scope": [
      "managed code registry",
      "complete dependency closure",
      "system-managed bare git repository",
      "temporary run folder execution contract",
      "long-running managed runner"
    ]
  },
  "artifacts": [
    {
      "path": "code/template_main_v1.py",
      "role": "inert code version; must not execute"
    },
    {
      "path": "code/template_main_v2.py",
      "role": "inert code version; must not execute"
    },
    {
      "path": "code/template_exploration_branch.py",
      "role": "inert exploratory branch version; must not execute"
    },
    {
      "path": "versions/version-index.json",
      "role": "available code-version choices"
    },
    {
      "path": "selection/selection-plan.json",
      "role": "operator selection and execution-mode comparison"
    },
    {
      "path": "records/run-record-selected-version.json",
      "role": "run provenance record for selected version; not observed loader evidence"
    }
  ],
  "fixture_questions": [
    "Is selecting an older code version or branch variant a real experiment workflow, not only retrospective provenance?",
    "Is tracking-only useful enough, or does the first useful product need load-and-run behavior?",
    "What isolation is required before running a selected version?",
    "Which code-version metadata must be user-declared instead of inferred from arbitrary imports?"
  ]
}
